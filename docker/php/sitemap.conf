# /sitemap.xml é gerado pelo PHP (sitemap.php): os buscadores esperam o endereço .xml.
<Directory /var/www/html>
    <IfModule mod_rewrite.c>
        RewriteEngine On
        RewriteRule ^sitemap\.xml$ /sitemap.php [L]
    </IfModule>
</Directory>
